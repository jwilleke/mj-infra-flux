# unencrypted secrets
.env.secret*
.env*
*.secrets
*.secret

# dockeronfigjson files contain secrets (*.dockeronfigjson.encrypted are okay)
*.dockeronfigjson

# these are encrypted .env secrets encrypted with SOPS+age
!.env.*.encrypted
# these are other files encrypted with SOPS+age
!*.dockeronfigjson.encrypted

# age key files cannot be committed:
*.agekey

# this one is generated by a script as a temp file - no need to commit:
scripts/ghcr.dockeronfigjson.encrypted

# IMPORTANT: Never commit files with actual secrets in plaintext
# Use SOPS encryption or Kubernetes secrets created directly in cluster
# See SECURITY-INCIDENT.md for lessons learned

# .vscode
.vscode/*
dev-notes.md

# Private/local configuration files
private/
