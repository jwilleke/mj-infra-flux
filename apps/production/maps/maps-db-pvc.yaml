apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maps-db-pvc
  namespace: maps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ""
  volumeName: maps-db-pv
