# Hourly SQLite backup of the yourphr health-records database to NAS.
# Uses sqlite3 .backup (hot backup — consistent regardless of open transactions).
# NAS path: 192.168.68.41:/var/nfs/shared/jims → /mnt/tank/jims/data/yourphr-backup/
# Retains 7 daily backups by filename (YYYYMMDD); keeps the latest as yourphr-latest.db.
# Image: keinos/sqlite3 (Alpine + sqlite3 pre-installed) — avoids runtime apk install,
# which fails when the APK CDN resolves to IPv6 and pod networking has no global IPv6 route.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: yourphr-backup
  namespace: yourphr
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: backup
              image: keinos/sqlite3:latest
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  DB=/data/fasten.db
                  DEST=/nas-backup
                  DATE=$(date +%Y%m%d)
                  echo "Backing up $DB → $DEST/yourphr-$DATE.db"
                  sqlite3 "$DB" ".backup $DEST/yourphr-$DATE.db"
                  cp "$DEST/yourphr-$DATE.db" "$DEST/yourphr-latest.db"
                  # Prune backups older than 7 days
                  find "$DEST" -name "yourphr-2*.db" -mtime +7 -delete
                  echo "Backup complete."
              volumeMounts:
                - name: yourphr-data
                  mountPath: /data
                  readOnly: true
                - name: nas-backup
                  mountPath: /nas-backup
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 128Mi
          volumes:
            - name: yourphr-data
              persistentVolumeClaim:
                claimName: yourphr-data
            - name: nas-backup
              hostPath:
                path: /mnt/tank/jims/data/yourphr-backup
                type: DirectoryOrCreate
