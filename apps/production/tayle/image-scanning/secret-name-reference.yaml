# we need to tell kustomize to update the ImageRepository CRD's secretRef/name value in the ImageRepository
# per https://github.com/kubernetes-sigs/kustomize/tree/master/examples/transformerconfigs#name-reference-transformer
nameReference:
  - kind: Secret
    fieldSpecs:
      - kind: ImageRepository
        path: spec/secretRef/name
