apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yourphr-ts-data
  namespace: yourphr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
