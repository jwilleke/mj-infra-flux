apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: landingpage-members
  namespace: landingpage
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.priority: "100"
    # Redirect /members to Authentik user library instead of serving a separate Members page
    traefik.ingress.kubernetes.io/router.middlewares: landingpage-redirect-to-authentik-library@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - nerdsbythehour.com
    secretName: landingpage-tls
  rules:
  - host: nerdsbythehour.com
    http:
      paths:
      - path: /members
        pathType: Prefix
        backend:
          service:
            name: landingpage
            port:
              number: 3000
