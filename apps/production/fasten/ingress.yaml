# fasten.nerdsbythehour.com — internal only (no public Cloudflare DNS record).
# LAN-reachable, authentik-authenticated (forward-auth tier).
# See docs/access/access.md and mj-infra-flux#99.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fasten-ingress
  namespace: fasten
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: fasten-authentik-forwardauth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - fasten.nerdsbythehour.com
      secretName: fasten-nerdsbythehour-com-tls
  rules:
    - host: fasten.nerdsbythehour.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fasten
                port:
                  number: 8080
