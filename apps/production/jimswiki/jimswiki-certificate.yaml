apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jimswiki-cert
  namespace: jimswiki
spec:
  secretName: jimswiki-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - nerdsbythehour.com
