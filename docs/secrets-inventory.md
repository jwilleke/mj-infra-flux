# Secrets Inventory

Audit of all `Secret` resources in the cluster. **Last refreshed: 2026-05-22** (covering the further #77 item 1 migration work).

The categorization tells you, for any Secret you find, **where its source of truth lives** — the most common question when investigating drift, doing a recovery rebuild, or onboarding a new agent.

## Categories

A Secret falls into exactly one of:

| Category | Source of truth | Action on cluster rebuild |
| --- | --- | --- |
| **Auto-generated** | Controller (cert-manager, k3s, k8s SA controller) | Re-issued automatically |
| **Helm-release state** | Helm itself (chart values + release history) | Re-created when the chart is reapplied |
| **In-repo (SOPS)** | `mj-infra-flux@HEAD`, decrypted by Flux's kustomize-controller via `flux-system/sops-age` | Re-applied by Flux on next reconcile |
| **Bootstrap** | Manual one-time apply — must be re-created out-of-band before Flux can do anything | Manual apply (chicken-and-egg) |
| **Hand-applied (drift)** | Operator memory — should be migrated to in-repo SOPS | Re-create or recover from backup |
| **Stale** | Nothing — orphaned, candidate for deletion | n/a |

## Cluster inventory

### Auto-generated

Issued and refreshed by controllers. Safe to delete — they regenerate.

- All `kubernetes.io/tls` Secrets — issued by **cert-manager** from `Certificate` resources via Let's Encrypt (HTTP-01 or DNS-01). Names end in `-tls` or `-cert`. Across ~15 namespaces (one per app + cert-manager / flux-system).
- `kubernetes.io/service-account-token` Secrets (`*-sa-token`) — issued by Kubernetes' own service-account controller.
- `cert-manager/cert-manager-webhook-ca` — internal CA for cert-manager's admission webhook.
- `cert-manager/letsencrypt-{production,staging}-issuer-secret-key` — ACME account keys, generated by cert-manager when the `ClusterIssuer` is first applied.
- `kube-system/k3s-serving` — k3s kube-apiserver serving cert.
- `kube-system/deby.node-password.k3s` — k3s node-join bootstrap password (set by k3s installer).

### Helm-release state

Track Helm release history. Not credentials per se. Don't migrate.

- `kube-system/sh.helm.release.v1.traefik.v{1..5}` and `traefik-crd.v{1..3}` — Helm release history for the bundled k3s Traefik chart.
- `kube-system/chart-values-{traefik,traefik-crd}` — values consumed by k3s's `HelmChart` controller.
- `authentik/sh.helm.release.v1.authentik.v1` — Authentik chart release history (Authentik is the one Helm-managed application — see AGENTS.md "Migrate remaining Helm deployments to Kustomize" TODO).

### In-repo (SOPS)

Source of truth lives in `mj-infra-flux@HEAD`. All carry `kustomize.toolkit.fluxcd.io/name=apps` or `=infra-configs` label. Encryption: SOPS with age recipient `age1sr8j9p87wuuqfnmharzqqnwj76yyc6mu5j3r5t7sr3j88wzn8exqwy6jhj`.

| Secret | Used by | In-repo location |
| --- | --- | --- |
| `authentik/authentik-secrets` | helmrelease `valuesFrom` (`secret_key` + `postgresql_password`); chart renders the downstream `authentik` + `authentik-postgresql` Secrets | `apps/production/authentik/authentik-secrets.sops.yaml` (migrated 2026-05-22 in `mj-infra-flux@55367d2`; closes #77 + #82) |
| `cert-manager/cloudflare-api-token` | live `ClusterIssuer/letsencrypt-{production,staging}` DNS-01 solver | `apps/production/cert-manager/cloudflare-api-token.sops.yaml` (migrated 2026-05-22 in `mj-infra-flux@b993868`) |
| `cloudflared/cloudflared-token` | `cloudflared` Deployment — Cloudflare Tunnel TUNNEL_TOKEN | `apps/production/cloudflared/.env.secret.cloudflared.encrypted` (kustomize secretGenerator) |
| `database/postgresql-secret` | Shared PostgreSQL StatefulSet | `apps/production/database/` (secretGenerator) |
| `flux-system/flux-system-git-auth` | `GitRepository/flux-system` `secretRef` — fluxcdbot git push creds | `apps/production/image-automation/flux-system-git-auth.sops.yaml` (Secret manifest) |
| `flux-system/ghcr-geohazardwatch` | `ImageRepository/geohazardwatch` `secretRef` — GHCR pull for image-reflector-controller | `apps/production/image-automation/geohazardwatch-ghcr.sops.yaml` (Secret manifest) |
| `flux-system/webhook-token-mk858c8447` | `infra-configs` Kustomization webhook receiver | `infrastructure/prod/configs/` (managed by infra-configs Kustomization) |
| `jimsmcp/ghcr-jimsmcp` | `jimsmcp` Deployment `imagePullSecrets` — GHCR pull for the `jimsmcp` private image | `apps/production/jimsmcp/ghcr-jimsmcp.sops.yaml` (Secret manifest) |
| `monitoring/alertmanager-secrets-*` | `alertmanager` Deployment volume mount — `gmail_app_password` for SMTP auth | `apps/production/monitoring/prometheus-alertmanager/.env.secret.alertmanager.encrypted` (kustomize secretGenerator; hash suffix appended at apply time) |
| `teslamate/teslamate-secret` | `teslamate` Deployment | `apps/production/teslamate/` (secretGenerator) |

### Bootstrap (legitimately hand-applied)

These have a chicken-and-egg dependency on Flux itself or on cluster bootstrap. Cannot live in this repo.

| Secret | Why out-of-band |
| --- | --- |
| `flux-system/sops-age` | Decrypts this repo's Secrets. Self-referential if encrypted. Apply via `kubectl create -n flux-system secret generic sops-age --from-file=age.agekey=…`. Nightly backup to `/deby-backup/.../flux-sops-age.key`; `backup-deby-verify` monitors; restore in `RESTORE.md` §6a. |

### Hand-applied (drift candidates)

*None.* All operationally-required hand-applied Secrets are migrated; the two prometheus orphans were deleted.

The Authentik trio previously listed here was re-classified 2026-05-22 (#82). Only `authentik-secrets` was hand-applied; the other two (`authentik`, `authentik-postgresql`) carry `helm.toolkit.fluxcd.io/*` labels — Helm-chart-managed via Flux, derived from `authentik-secrets` at each chart render.

Prior inventory filtered only by `kustomize.toolkit.fluxcd.io/*` and missed the helm-controller label class.

### Stale (delete candidates)

*None.*

The two previously-listed orphans (`default/prometheus-secrets-g224cmcfd8`, `default/prometheus-sa-token`) were deleted 2026-05-22 — zero live consumers verified pre-delete.

The four previously-listed `default/*-tls` Secrets were re-classified as auto-generated (legitimate cert-manager certs for Ingresses in `default` ns; not stale).

## cert-manager ClusterIssuer drift — resolved 2026-05-22 (#81)

Surfaced during `cloudflare-api-token` migration: live ClusterIssuers had DNS-01 Cloudflare config that Git didn't declare. Resolved in `mj-infra-flux@33353c5` by moving the ClusterIssuers into `apps/production/cert-manager/` (now Flux-reconciled) with the live spec preserved byte-identical.

Both ClusterIssuers now carry `kustomize.toolkit.fluxcd.io/*` labels; delete-and-reconcile acceptance verified on the staging issuer.

**Still hand-applied** (separate concern, NOT covered by #81): the cert-manager **workload** (Deployment, cainjector, webhook, CRDs, RBAC). Lives under `apps/base/cert-manager/`, never wired into a Flux Kustomization, 477+ d old, predates the GitOps repo.

See `apps/base/cert-manager/README-cert-manager.md` "Ownership split" section. Worth its own narrowly-scoped issue when there's a reason to migrate.

## Recovery procedure (if `deby` is rebuilt)

In order:

1. **Bootstrap age key**: restore `~/.config/sops/age/keys.txt` from the `/deby-backup/.../flux-sops-age.key` artifact, then `kubectl -n flux-system create secret generic sops-age --from-file=age.agekey=…`.
2. **`flux bootstrap`** against this repo. Once flux-system reconciles, *all* in-repo SOPS Secrets are re-applied automatically.
3. **Authentik downstream Secrets** (`authentik`, `authentik-postgresql`) are then rendered by the Helm chart from values read out of `authentik-secrets` via `valuesFrom`. No manual step.
4. The ClusterIssuers themselves are now Flux-reconciled (#81 resolved 2026-05-22) and will be re-applied automatically alongside the Secret in step 2 — no separate operator action.
5. **Confirm cert-manager re-issues TLS Secrets** by checking `kubectl get certificate -A` shows `Ready: True` for each. ACME rate-limits apply if many certs renew at once.

## Open follow-ups

- **Bring the cert-manager workload into Flux** — separate from #81 (which only resolved the ClusterIssuer + Secret drift). The Deployment / cainjector / webhook / CRDs are still hand-applied (477+ d, predates the GitOps repo). File when there's a reason to do it.
- **Operator-held off-disk copy of the sops-age key** — `deby-backup` is local-only (not geo-redundant). Out-of-band, can't be automated; password-manager or offline drive.
